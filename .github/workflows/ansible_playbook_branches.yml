name: ansible-playbook-branches
on:
  - push
  - workflow_dispatch
jobs:
  ## run build ###################################################
  # begin - build-alpine_3.18-linux/amd64
  aef1e3e5895d14adccfebb804f6c2f8bc704d2d346cc0d797675b16f86722f97d:
    name: alpine_3.18,linux/amd64
    runs-on: ubuntu-latest
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"alpine_3.18","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/amd64 \
            target_tag=latest \
            is_tag=false
  # end - build-alpine_3.18-linux/amd64
  # begin - build-alpine_3.18-linux/arm64
  a8e50f28b87529867e094666995e8f7cd143badc19ac5df19b35bccdbfac208a4:
    name: alpine_3.18,linux/arm64
    runs-on: arm64
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"alpine_3.18","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/arm64 \
            target_tag=latest \
            is_tag=false
  # end - build-alpine_3.18-linux/arm64
  # begin - build-alpine_3.19-linux/amd64
  a57f1ca4c92dbd5f979e3069a1c8403da3202956058c648d688673aa4b877493c:
    name: alpine_3.19,linux/amd64
    runs-on: ubuntu-latest
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"alpine_3.19","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/amd64 \
            target_tag=latest \
            is_tag=false
  # end - build-alpine_3.19-linux/amd64
  # begin - build-alpine_3.19-linux/arm64
  ac4dac4ec21c10970c9b024da001747b5af07407f9f79e4d198728efc690a8e8e:
    name: alpine_3.19,linux/arm64
    runs-on: arm64
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"alpine_3.19","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/arm64 \
            target_tag=latest \
            is_tag=false
  # end - build-alpine_3.19-linux/arm64
  # begin - build-archlinux_latest-linux/amd64
  a0f79f9d7af44bec254a9d55984311e2af4a52afe8c11e6ab3473a8c6393d62cd:
    name: archlinux_latest,linux/amd64
    runs-on: ubuntu-latest
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"archlinux_latest","platforms":["linux/amd64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/amd64 \
            target_tag=latest \
            is_tag=false
  # end - build-archlinux_latest-linux/amd64
  # begin - build-debian_bookworm-linux/amd64
  a3072614204f41306fb19315027c4cd2ece2abf0bb4a899848a70aac651f5d644:
    name: debian_bookworm,linux/amd64
    runs-on: ubuntu-latest
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"debian_bookworm","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/amd64 \
            target_tag=latest \
            is_tag=false
  # end - build-debian_bookworm-linux/amd64
  # begin - build-debian_bookworm-linux/arm64
  a9593c3b072559ae021569edec4def555688926446de61048ed2210b3726b945e:
    name: debian_bookworm,linux/arm64
    runs-on: arm64
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"debian_bookworm","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/arm64 \
            target_tag=latest \
            is_tag=false
  # end - build-debian_bookworm-linux/arm64
  # begin - build-debian_bookworm-slim-linux/amd64
  a244d78324ff2408b74f026c5b01d206e00c4d28e75bf26bb2692a63f008ed982:
    name: debian_bookworm-slim,linux/amd64
    runs-on: ubuntu-latest
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"debian_bookworm-slim","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/amd64 \
            target_tag=latest \
            is_tag=false
  # end - build-debian_bookworm-slim-linux/amd64
  # begin - build-debian_bookworm-slim-linux/arm64
  a98a0e548a6229f57214d52c4bf203d5b7ec85e46d4b8c9f979700d29c7319d91:
    name: debian_bookworm-slim,linux/arm64
    runs-on: arm64
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"debian_bookworm-slim","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/arm64 \
            target_tag=latest \
            is_tag=false
  # end - build-debian_bookworm-slim-linux/arm64
  # begin - build-rockylinux_8-linux/amd64
  aae23814c2edfdf7d2c47039efff82567b6bd9ecbb4fa531df79ae55a5059773e:
    name: rockylinux_8,linux/amd64
    runs-on: ubuntu-latest
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"rockylinux_8","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/amd64 \
            target_tag=latest \
            is_tag=false
  # end - build-rockylinux_8-linux/amd64
  # begin - build-rockylinux_8-linux/arm64
  a5fa36ff0bf7c99154bd59daaf78c6d9415c3606e079670d4ed537c031d2d3c5d:
    name: rockylinux_8,linux/arm64
    runs-on: arm64
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"rockylinux_8","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/arm64 \
            target_tag=latest \
            is_tag=false
  # end - build-rockylinux_8-linux/arm64
  # begin - build-rockylinux_9-linux/amd64
  ae0fa1b46c00021455937574ae88643c50f3391a269e31b8c88d417933bd3522d:
    name: rockylinux_9,linux/amd64
    runs-on: ubuntu-latest
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"rockylinux_9","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/amd64 \
            target_tag=latest \
            is_tag=false
  # end - build-rockylinux_9-linux/amd64
  # begin - build-rockylinux_9-linux/arm64
  a071049dec0ba042ff492a243d3f3b9c06d95c2d2b794e033da82eaf389c3f66f:
    name: rockylinux_9,linux/arm64
    runs-on: arm64
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"rockylinux_9","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/arm64 \
            target_tag=latest \
            is_tag=false
  # end - build-rockylinux_9-linux/arm64
  # begin - build-ubuntu_jammy-linux/amd64
  a794e56abf3af4e23a47b368ef9eb7b4b3e42686af0f19eb206b74c04b0f05621:
    name: ubuntu_jammy,linux/amd64
    runs-on: ubuntu-latest
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"ubuntu_jammy","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/amd64 \
            target_tag=latest \
            is_tag=false
  # end - build-ubuntu_jammy-linux/amd64
  # begin - build-ubuntu_jammy-linux/arm64
  abad598078f7832c70c6536eac66638a0731ce6b0e990ba15a19f204d97440f5a:
    name: ubuntu_jammy,linux/arm64
    runs-on: arm64
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"ubuntu_jammy","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/arm64 \
            target_tag=latest \
            is_tag=false
  # end - build-ubuntu_jammy-linux/arm64
  # begin - build-ubuntu_mantic-linux/amd64
  ab8aba887c1ad64cbc9486d63647986924fab2411437d88fc9e3cee8e5afdec0f:
    name: ubuntu_mantic,linux/amd64
    runs-on: ubuntu-latest
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"ubuntu_mantic","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/amd64 \
            target_tag=latest \
            is_tag=false
  # end - build-ubuntu_mantic-linux/amd64
  # begin - build-ubuntu_mantic-linux/arm64
  a6e8a85b09384ea84ea34ada4684da3edf414528cf0c2e2cc90eb2475eb724fdd:
    name: ubuntu_mantic,linux/arm64
    runs-on: arm64
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"ubuntu_mantic","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/arm64 \
            target_tag=latest \
            is_tag=false
  # end - build-ubuntu_mantic-linux/arm64
  # begin - build-ubuntu_noble-linux/amd64
  a30bc0c91805799c95f5f2d45e88b4b78f4de3f70b6bb7e3325dad0be103e4b76:
    name: ubuntu_noble,linux/amd64
    runs-on: ubuntu-latest
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"ubuntu_noble","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/amd64 \
            target_tag=latest \
            is_tag=false
  # end - build-ubuntu_noble-linux/amd64
  # begin - build-ubuntu_noble-linux/arm64
  a621db30e238050790f022a2e6e96cea3ba90040fcef49b6b8b266f6e2ce45259:
    name: ubuntu_noble,linux/arm64
    runs-on: arm64
    steps:
      - name: checkout=self
        uses: actions/checkout@v4
      - name: task=3.35.1
        uses: arduino/setup-task@v1
        with:
          version: 3.35.1
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: checkout=taskfiles@feature/arm
        uses: actions/checkout@v4
        with:
          path: taskfiles
          repository: bradfordwagner/taskfiles
          ref: feature/arm
      
      # https://github.com/chrisdickinson/setup-yq/blob/latest/action.yml
      - uses: chrisdickinson/setup-yq@v1.0.1
        name: yq=v4.43.1
        with:
          yq-version: v4.43.1
      - uses: docker/login-action@v3
        name: login=ghcr.io
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      
      - name: task=branches
        run: |
          build='{"os":"ubuntu_noble","platforms":["linux/amd64","linux/arm64"]}'
          task -t ./taskfiles/tasks/containers/ansible.yml \
            build \
            build=${build} \
            platform=linux/arm64 \
            target_tag=latest \
            is_tag=false
  # end - build-ubuntu_noble-linux/arm64
  ## manifest tool ###############################################
  # begin - manifest-alpine_3.18
  a3f81e606d5902da8fe2af62e850773377b9e4b2bcff71f9a835f3e0cbd1a23da:
    name: alpine_3.18
    runs-on: ubuntu-latest
    needs:
      # requires first character to be a letter
      # use sha256 of build os and platform
      - aef1e3e5895d14adccfebb804f6c2f8bc704d2d346cc0d797675b16f86722f97d
      # requires first character to be a letter
      # use sha256 of build os and platform
      - a8e50f28b87529867e094666995e8f7cd143badc19ac5df19b35bccdbfac208a4
    steps:
      - name: test
        run: echo "test"
  # end - manifest-alpine_3.18
  ## manifest tool ###############################################
  # begin - manifest-alpine_3.19
  a49bdf2ec0ee8540492558e481f3568bbaa89c3134c74ff9bb7d496bfcbef833d:
    name: alpine_3.19
    runs-on: ubuntu-latest
    needs:
      # requires first character to be a letter
      # use sha256 of build os and platform
      - a57f1ca4c92dbd5f979e3069a1c8403da3202956058c648d688673aa4b877493c
      # requires first character to be a letter
      # use sha256 of build os and platform
      - ac4dac4ec21c10970c9b024da001747b5af07407f9f79e4d198728efc690a8e8e
    steps:
      - name: test
        run: echo "test"
  # end - manifest-alpine_3.19
  ## manifest tool ###############################################
  # begin - manifest-archlinux_latest
  abb9b84b6e63629fa3380996a7236aaa36cbc5dd9275be5dbbaa0f909e8e7b10d:
    name: archlinux_latest
    runs-on: ubuntu-latest
    needs:
      # requires first character to be a letter
      # use sha256 of build os and platform
      - a0f79f9d7af44bec254a9d55984311e2af4a52afe8c11e6ab3473a8c6393d62cd
    steps:
      - name: test
        run: echo "test"
  # end - manifest-archlinux_latest
  ## manifest tool ###############################################
  # begin - manifest-debian_bookworm
  abb546b6b90d95a9ca6039807069125d00bc3e4a69ef3393f9a65a7147bd76a12:
    name: debian_bookworm
    runs-on: ubuntu-latest
    needs:
      # requires first character to be a letter
      # use sha256 of build os and platform
      - a3072614204f41306fb19315027c4cd2ece2abf0bb4a899848a70aac651f5d644
      # requires first character to be a letter
      # use sha256 of build os and platform
      - a9593c3b072559ae021569edec4def555688926446de61048ed2210b3726b945e
    steps:
      - name: test
        run: echo "test"
  # end - manifest-debian_bookworm
  ## manifest tool ###############################################
  # begin - manifest-debian_bookworm-slim
  a7a8cbed9d9a2e94fb56e35155cc8fe61fbc1f6c89357b7eb10b509068e63acec:
    name: debian_bookworm-slim
    runs-on: ubuntu-latest
    needs:
      # requires first character to be a letter
      # use sha256 of build os and platform
      - a244d78324ff2408b74f026c5b01d206e00c4d28e75bf26bb2692a63f008ed982
      # requires first character to be a letter
      # use sha256 of build os and platform
      - a98a0e548a6229f57214d52c4bf203d5b7ec85e46d4b8c9f979700d29c7319d91
    steps:
      - name: test
        run: echo "test"
  # end - manifest-debian_bookworm-slim
  ## manifest tool ###############################################
  # begin - manifest-rockylinux_8
  a7a230d57ab0acb5285181157f0979876bbe0b5a7692a958f1911fa09c0ac07f7:
    name: rockylinux_8
    runs-on: ubuntu-latest
    needs:
      # requires first character to be a letter
      # use sha256 of build os and platform
      - aae23814c2edfdf7d2c47039efff82567b6bd9ecbb4fa531df79ae55a5059773e
      # requires first character to be a letter
      # use sha256 of build os and platform
      - a5fa36ff0bf7c99154bd59daaf78c6d9415c3606e079670d4ed537c031d2d3c5d
    steps:
      - name: test
        run: echo "test"
  # end - manifest-rockylinux_8
  ## manifest tool ###############################################
  # begin - manifest-rockylinux_9
  a7aaf9367aace7ba26ce956e66bb172d3ce143cd706a4f52ef78a0c28feedb512:
    name: rockylinux_9
    runs-on: ubuntu-latest
    needs:
      # requires first character to be a letter
      # use sha256 of build os and platform
      - ae0fa1b46c00021455937574ae88643c50f3391a269e31b8c88d417933bd3522d
      # requires first character to be a letter
      # use sha256 of build os and platform
      - a071049dec0ba042ff492a243d3f3b9c06d95c2d2b794e033da82eaf389c3f66f
    steps:
      - name: test
        run: echo "test"
  # end - manifest-rockylinux_9
  ## manifest tool ###############################################
  # begin - manifest-ubuntu_jammy
  a6917b1c15024350c73898dd7c44e24defffce76605d226213bbc59a2313a15de:
    name: ubuntu_jammy
    runs-on: ubuntu-latest
    needs:
      # requires first character to be a letter
      # use sha256 of build os and platform
      - a794e56abf3af4e23a47b368ef9eb7b4b3e42686af0f19eb206b74c04b0f05621
      # requires first character to be a letter
      # use sha256 of build os and platform
      - abad598078f7832c70c6536eac66638a0731ce6b0e990ba15a19f204d97440f5a
    steps:
      - name: test
        run: echo "test"
  # end - manifest-ubuntu_jammy
  ## manifest tool ###############################################
  # begin - manifest-ubuntu_mantic
  a96743810a0f10b320f0a52ee76dac4d7b16ace1c0a158482511429caff58509b:
    name: ubuntu_mantic
    runs-on: ubuntu-latest
    needs:
      # requires first character to be a letter
      # use sha256 of build os and platform
      - ab8aba887c1ad64cbc9486d63647986924fab2411437d88fc9e3cee8e5afdec0f
      # requires first character to be a letter
      # use sha256 of build os and platform
      - a6e8a85b09384ea84ea34ada4684da3edf414528cf0c2e2cc90eb2475eb724fdd
    steps:
      - name: test
        run: echo "test"
  # end - manifest-ubuntu_mantic
  ## manifest tool ###############################################
  # begin - manifest-ubuntu_noble
  a5d5f1920cf1e6f8887594eeb07d9aa75150a8973494c03922f5efa94b3890b72:
    name: ubuntu_noble
    runs-on: ubuntu-latest
    needs:
      # requires first character to be a letter
      # use sha256 of build os and platform
      - a30bc0c91805799c95f5f2d45e88b4b78f4de3f70b6bb7e3325dad0be103e4b76
      # requires first character to be a letter
      # use sha256 of build os and platform
      - a621db30e238050790f022a2e6e96cea3ba90040fcef49b6b8b266f6e2ce45259
    steps:
      - name: test
        run: echo "test"
  # end - manifest-ubuntu_noble
